# 小算例，用于快速回归与重启验证
Nx = 32
Ny = 32
Nz = 32
dt = 0.002
n_steps = 5

kT        = 0.25
radius    = 3.2
xi        = 1.0
ratio_eta = 50.0

# 边界固定为 x/y 周期、z 向上下无滑移硬壁，不是配置项。

init_file     = out/init.fpd
out_dir       = out
run_name      = run
interval_ckpt = 1
interval_log  = 1000000
seed          = 20260905
